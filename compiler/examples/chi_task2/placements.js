// Complete the definition of the placement object of the timeline layer.
// The placement object specifies for each row in the data transform result
// a rectangular area on the canvas that this row will occupy.
// You need to specify the centroid_x, centroid_y and width and height of this box.
// Each value can be specified as either a constant number, or a column in the data
// transform result.
// On the teamline canvas, 'x' and 'y' columns are centroid coordinates of each game.
// The width and height are respectively 160 and 130. See examples in the warmup
// exercise for exact syntax.
var teamTimelinePlacement = {};

module.exports = {
    teamTimelinePlacement : teamTimelinePlacement
};
